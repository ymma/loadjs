{
  "name": "loadys",
  "version": "0.1.6",
  "description": "A loadys helper",
  "main": "dist/loadys.js",
  "scripts": {
    "build": "npm run prettier && rm -rf ./dist && babel src -d dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "prettier --write ./src/*"
  },
  "keywords": [],
  "author": "ymma",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ymma/loadys.git"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0"
  }
}
